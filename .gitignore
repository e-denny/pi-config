# Secrets — never commit
agent/auth.json
agent/models-store.json

# Machine-specific
agent/trust.json

# Transient / generated
agent/sessions/
agent/npm/
web-search-cache/
agent/extensions/herdr-agent-state.ts

# Extension dependencies
agent/extensions/**/node_modules/

# pi-prompt-box keeps its own repo
agent/extensions/pi-prompt-box/
